{
	"name": "gildedrose-ts-vitest",
	"version": "1.0.0",
	"license": "MIT",
	"scripts": {
    "all": "npm install && npm run build && npm test",
		"build": "tsc",
		"test": "vitest run",
		"test:watch": "vitest",
		"test:coverage": "vitest run --coverage"
	},
	"devDependencies": {
		"typescript": "5.7.2",
		"vitest": "2.1.5"
	}
}
